const getCurrent = require('./getCurrent');
const uploadAvatar = require('./uploadAvatar');
const updatePassword = require('./updatePassword');
const saveAvatarUrl = require('./saveAvatarUrl')

module.exports = {
    getCurrent,
    uploadAvatar,
    updatePassword,
    saveAvatarUrl
}