const { User } = require('./user');
const { Task } = require('./task');
const { Board } = require('./board');
const { ColorSet } = require('./colorSet');

module.exports = {
    User,
    Task,
    Board,
    ColorSet
};